<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jun.xiaoquren"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <application android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <!-- android:allowBackup="true" -->
        <!-- android:theme="@android:style/Theme.NoTitleBar.Fullscreen" > -->
        
        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Anim_style2" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".PersonalSettingActivity" android:theme="@style/Anim_style2" />
        <activity android:name=".XiaoQuSearchActivity" android:theme="@style/Anim_style2" />
        <activity android:name=".XiaoQuItemActivity" android:theme="@style/Anim_style2" />
        
    </application>

</manifest>
